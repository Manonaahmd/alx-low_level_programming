#include <stdio.h> 

/**
 * main -prints a given phrase 
 * return: 0 if exited properly, non-zero otherwise
 */

int  main(void) 

{
	puts("\" programming is like building a multilingul pazzel");
	return (0);
}
